"""Configuração do pytest.

A simples presença deste arquivo na raiz faz o pytest inserir o diretório do
projeto no `sys.path`, o que permite que os testes usem `from src... import`
sem precisar instalar o pacote.
"""
