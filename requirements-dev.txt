# Dependências usadas apenas em desenvolvimento.
# Instale junto das de produção: pip install -r requirements.txt -r requirements-dev.txt
pytest>=8.0.0

# Gera um relatório de testes em HTML, útil para visualizar o resultado
# sem depender da saída do terminal.
pytest-html>=4.1.0

# Linter e formatador. As regras ficam em ruff.toml.
ruff>=0.16.0
